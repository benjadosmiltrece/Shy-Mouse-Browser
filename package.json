{
  "name": "my-electron-browser",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . my-browser --overwrite --platform=linux --arch=x64 --icon=assets/icon.png --prune=true --out=release-build"
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "electron-packager": "^17.1.2"
  }
}
